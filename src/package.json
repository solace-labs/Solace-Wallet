{
  "name": "solace-sdk",
  "version": "2.0.6",
  "description": "SDK to interact with the Solace Program",
  "main": "./dist/esm/sdk.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "prepublishOnly": "rm -rf dist && npx tsc -p tsconfig.json"
  },
  "files": [
    "./dist"
  ],
  "repository": {
    "type": "git",
    "url": "github.com/d3fkon/solace-wallet"
  },
  "keywords": [
    "solace",
    "rust",
    "solana"
  ],
  "author": "Ashwin Prasad",
  "license": "MIT",
  "dependencies": {
    "@solana/spl-token": "^0.3.4",
    "anchor-rn": "^0.18.2",
    "bs58": "^5.0.0"
  }
}
